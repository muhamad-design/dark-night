# This folder sits inside an Obsidian vault that auto-commits, so build output
# and dependencies must be excluded before anything is installed.
node_modules/
popup/dist/
.vite/
*.tsbuildinfo
# Hand-packed store uploads - built from the tree, never the source of truth.
*.zip
.DS_Store

# Session handoff / assistant notes and tooling config - not for the public repo.
CLAUDE.md
NEXT_SESSION.md
.claude/
